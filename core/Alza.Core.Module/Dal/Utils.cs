using System.Collections.Generic;
using System.Data;

namespace Alza.Core.Module.Dal
{
    public class Utils
    {
        /// <summary>
		/// Vytvoří tabulku s identifikátory.
		/// </summary>
		/// <param name="ids">identifikátory.</param>
		/// <returns>Tabulka s id's.</returns>
		//public static DataTable GetIdentifiersDataTable(List<int> ids)
  //      {
  //          // Jsou zadané země?
  //          if (ids.Count < 1)
  //              return null;

  //          // Vytvoř tabulku s výrobci
  //          DataTable table = new DataTable("Identifiers");
  //          table.Columns.Add("Id", typeof(int));

  //          // Naplň tabulku s výrobci
  //          foreach (int id in ids)
  //              table.Rows.Add(id);

  //          return table;
  //      }
    }
}
